# 護理排班系統

這是一個純前端專案，無需任何安裝或建置步驟。

## 專案說明

- 需求文件：`PRD.md`
- 產出目標：`index.html`（單一 HTML 檔案，包含所有 CSS 與 JavaScript）

## 技術規格

- 純 HTML + CSS + JavaScript
- 不使用任何框架或套件（不需要 npm install）
- 直接用瀏覽器開啟 `index.html` 即可執行

## 開發指引

實作時請完整閱讀 `PRD.md`，所有功能需求、排班規則、UI 規格均在其中定義。
